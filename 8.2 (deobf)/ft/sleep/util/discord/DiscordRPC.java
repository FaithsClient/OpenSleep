package ft.sleep.util.discord;

public enum DiscordRPC {
   simpsons$(0),
   arising$(1),
   sandra$(2);

   public int avoid$;
   public static DiscordRPC[] webcams$ = new DiscordRPC[]{simpsons$, arising$, sandra$};

   public DiscordRPC(int ibuzobap) {
      removeca.avoid$ = (int)ibuzobap;
   }
}
