package ft.sleep.util.discord;

public class DiscordEventHandlers {
   public DiscordEventHandlers2 threads$ = new DiscordEventHandlers2();

   public DiscordEventHandlers _advisor(ReadyCallback anymore) {
      peeing.threads$.ceramic$ = (ReadyCallback)anymore;
      return peeing;
   }

   public DiscordEventHandlers _saver(DisconnectedCallback munich) {
      shaft.threads$.broker$ = (DisconnectedCallback)munich;
      return shaft;
   }

   public DiscordEventHandlers _replace(ErroredCallback defense) {
      graphics.threads$.chaos$ = (ErroredCallback)defense;
      return graphics;
   }

   public DiscordEventHandlers _induced(JoinGameCallback adozupot) {
      apilezac.threads$.lucas$ = (JoinGameCallback)adozupot;
      return apilezac;
   }

   public DiscordEventHandlers _dialogue(SpectateGameCallback yinavena) {
      fidafitu.threads$.taste$ = (SpectateGameCallback)yinavena;
      return fidafitu;
   }

   public DiscordEventHandlers _enable(JoinRequestCallback yield) {
      holdem.threads$.diameter$ = (JoinRequestCallback)yield;
      return holdem;
   }

   public DiscordEventHandlers2 _pixels() {
      return glasses.threads$;
   }
}
