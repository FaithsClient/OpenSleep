package ft.sleep.util.discord;

public class DiscordRichPresence2 {
   public DiscordRichPresence studying$ = new DiscordRichPresence();

   public DiscordRichPresence2(String modeling) {
      washing.studying$.provides$ = (String)modeling;
   }

   public DiscordRichPresence2 _through(String sheet) {
      recent.studying$.removal$ = (String)sheet;
      return recent;
   }

   public DiscordRichPresence2 _sussex(long ecopomeg) {
      avivupic.studying$.denver$ = (long)ecopomeg;
      return avivupic;
   }

   public DiscordRichPresence2 _light(long higher) {
      guardian.studying$.links$ = (long)higher;
      return guardian;
   }

   public DiscordRichPresence2 _freeze(String opunicad, String ofocemum) {
      if (ofocemum != null && !((String)ofocemum).equalsIgnoreCase("") && opunicad == null) {
         throw new IllegalArgumentException("Image key must not be null when assigning a hover text.");
      } else {
         emibomiy.studying$.dressing$ = (String)opunicad;
         emibomiy.studying$.assign$ = (String)ofocemum;
         return emibomiy;
      }
   }

   public DiscordRichPresence2 _bundle(String gesededa, String uyulaneb) {
      if (uyulaneb != null && !((String)uyulaneb).equalsIgnoreCase("") && gesededa == null) {
         throw new IllegalArgumentException("Image key must not be null when assigning a hover text.");
      } else {
         ozeropan.studying$.resort$ = (String)gesededa;
         ozeropan.studying$.somerset$ = (String)uyulaneb;
         return ozeropan;
      }
   }

   public DiscordRichPresence2 _launched(String usacomay, int sazocega, int aneyimup) {
      ceropiso.studying$.highways$ = (String)usacomay;
      ceropiso.studying$.cameron$ = (int)sazocega;
      ceropiso.studying$.lloyd$ = (int)aneyimup;
      return ceropiso;
   }

   /** @deprecated */
   @Deprecated
   public DiscordRichPresence2 _excerpt(String poverty, String address, String merely) {
      kelkoo.studying$.settings$ = (String)poverty;
      kelkoo.studying$.orange$ = (String)address;
      kelkoo.studying$.terry$ = (String)merely;
      return kelkoo;
   }

   public DiscordRichPresence2 _adoption(String pasirovu, String obabonad) {
      agemanen.studying$.orange$ = (String)pasirovu;
      agemanen.studying$.terry$ = (String)obabonad;
      return agemanen;
   }

   /** @deprecated */
   @Deprecated
   public DiscordRichPresence2 _calling(boolean nonopivi) {
      alaromuy.studying$.kinda$ = nonopivi ? 1 : 0;
      return alaromuy;
   }

   public DiscordRichPresence _minimize() {
      return river.studying$;
   }
}
