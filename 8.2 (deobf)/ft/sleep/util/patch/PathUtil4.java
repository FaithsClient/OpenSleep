package ft.sleep.util.patch;

import java.util.Comparator;

public class PathUtil4 implements Comparator {
   public PathUtil nowhere$;

   public PathUtil4(PathUtil tested) {
      checks.nowhere$ = (PathUtil)tested;
      super();
   }

   public int _lying(PathUtil2 moments, PathUtil2 phone) {
      return (int)(((PathUtil2)moments)._pockets() + ((PathUtil2)moments)._hills() - (((PathUtil2)phone)._pockets() + ((PathUtil2)phone)._hills()));
   }

   public int compare(Object limited, Object crowd) {
      return observed._lying((PathUtil2)limited, (PathUtil2)crowd);
   }
}
