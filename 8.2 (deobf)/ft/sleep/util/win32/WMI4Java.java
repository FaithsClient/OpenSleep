package ft.sleep.util.win32;

import java.util.ArrayList;
import java.util.Arrays;
import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;

public class WMI4Java {
   public static String quoted$ = "\\r?\\n";
   public static String badly$ = "\\s+";
   public static String hitting$ = "Error calling ft.sleep.util.win32.WMI4Java";
   public String saturday$ = "*";
   public String allowed$ = ".";
   public boolean width$ = false;
   public List produce$ = null;
   public List smoke$ = null;

   public WMIStub _finances() {
      return (WMIStub)(ebayiyob.width$ ? new WMIVBScript() : new WMIPowerShell());
   }

   public static WMI4Java _begun() {
      return new WMI4Java();
   }

   public WMI4Java _deleted(String aredised) {
      titafeta.saturday$ = (String)aredised;
      return titafeta;
   }

   public WMI4Java _value(String iziliyov) {
      citageno.allowed$ = (String)iziliyov;
      return citageno;
   }

   public WMI4Java _johnny() {
      uyovepit.width$ = false;
      return uyovepit;
   }

   public WMI4Java _bored() {
      erugozum.width$ = true;
      return erugozum;
   }

   public WMI4Java _favor(List imutefiy) {
      ozogonas.produce$ = (List)imutefiy;
      return ozogonas;
   }

   public WMI4Java _ladies(List uvenuroy) {
      ubuleris.smoke$ = (List)uvenuroy;
      return ubuleris;
   }

   public List _dynamics() throws WMIException {
      Object surge = new ArrayList();
      Object clearing = browsing._finances()._coral(browsing.saturday$, browsing.allowed$);
      Object gospel = clearing.split("\\r?\\n");

      for(Object filing : gospel) {
         if (!filing.isEmpty() && !filing.startsWith("_")) {
            Object internet = filing.split("\\s+");
            surge.addAll(Arrays.asList(internet));
         }
      }

      Object var9 = new HashSet();
      var9.addAll(surge);
      surge.clear();
      surge.addAll(var9);
      return surge;
   }

   public List _funny(String lafuvume) throws WMIException {
      Object ogodivic = new ArrayList();
      Object degamoro = ahead._finances()._airfare((String)lafuvume, ahead.saturday$, ahead.allowed$);
      Object ifutepez = degamoro.split("\\r?\\n");

      for(Object sadozusu : ifutepez) {
         if (!sadozusu.isEmpty()) {
            ogodivic.add(sadozusu.trim());
         }
      }

      Object var9 = Arrays.asList("Equals", "GetHashCode", "GetType", "ToString");
      ogodivic.removeAll(var9);
      return ogodivic;
   }

   public Map _crime(WMIClass dining) {
      return aruba._william(((WMIClass)dining)._airports());
   }

   public Map _william(String obufilat) throws WMIException {
      Object dozidiya = new HashMap();
      String iburosol;
      if (isagalab.produce$ == null && isagalab.smoke$ == null) {
         iburosol = isagalab._finances()._signing((String)obufilat, isagalab.saturday$, isagalab.allowed$);
      } else {
         iburosol = isagalab._finances()._drilling((String)obufilat, isagalab.produce$, isagalab.smoke$, isagalab.saturday$, isagalab.allowed$);
      }

      Object aceguvof = iburosol.split("\\r?\\n");

      for(Object latusaca : aceguvof) {
         if (!latusaca.isEmpty()) {
            Object ipunugop = latusaca.split(":");
            if (ipunugop != null && ipunugop.length == 2) {
               dozidiya.put(ipunugop[0].trim(), ipunugop[1].trim());
            }
         }
      }

      return dozidiya;
   }

   public List _danish(WMIClass bopotimu) {
      return animadop._scenic(((WMIClass)bopotimu)._airports());
   }

   public List _scenic(String unisovem) throws WMIException {
      Object vetobape = new ArrayList();
      String ibibumor;
      if (zipodoti.produce$ == null && zipodoti.smoke$ == null) {
         ibibumor = zipodoti._finances()._signing((String)unisovem, zipodoti.saturday$, zipodoti.allowed$);
      } else {
         ibibumor = zipodoti._finances()._drilling((String)unisovem, zipodoti.produce$, zipodoti.smoke$, zipodoti.saturday$, zipodoti.allowed$);
      }

      Object uneyufaf = ibibumor.split("\\r?\\n\\r?\\n");

      for(Object cesiguce : uneyufaf) {
         Object feyenazi = cesiguce.split("\\r?\\n");
         Object ebetoyum = new HashMap();

         for(Object teyutuyi : feyenazi) {
            if (!teyutuyi.isEmpty()) {
               Object isovacir = teyutuyi.split(":");
               if (isovacir.length == 2) {
                  ebetoyum.put(isovacir[0].trim(), isovacir[1].trim());
               }
            }
         }

         vetobape.add(ebetoyum);
      }

      return vetobape;
   }

   public String _lawsuit(WMIClass scene) {
      return fighting._degree(((WMIClass)scene)._airports());
   }

   public String _degree(String speaks) throws WMIException {
      String revenge;
      if (polish.produce$ == null && polish.smoke$ == null) {
         revenge = polish._finances()._signing((String)speaks, polish.saturday$, polish.allowed$);
      } else {
         revenge = polish._finances()._drilling((String)speaks, polish.produce$, polish.smoke$, polish.saturday$, polish.allowed$);
      }

      return revenge;
   }
}
