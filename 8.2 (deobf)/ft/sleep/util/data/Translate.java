package ft.sleep.util.data;

import ft.sleep.util.animation.AnimationUtils;

public class Translate {
   public float flickr$ = Float.intBitsToFloat(0);
   public float drain$ = Float.intBitsToFloat(0);
   public float teacher$ = Float.intBitsToFloat(0);
   public float equity$ = Float.intBitsToFloat(0);
   public float believe$ = Float.intBitsToFloat(0);
   public float murder$ = Float.intBitsToFloat(0);
   public float scene$ = Float.intBitsToFloat(0);
   public float touring$ = Float.intBitsToFloat(0);
   public float lawyers$ = Float.intBitsToFloat(0);

   public void _achieved(float odagalad, float ofodomev, float zubaficu) {
      if (repovuza.flickr$ == Float.intBitsToFloat(0) || repovuza.flickr$ == odagalad || repovuza.scene$ != odagalad) {
         repovuza.equity$ = repovuza.flickr$;
         repovuza.scene$ = (float)odagalad;
      }

      if (repovuza.drain$ == Float.intBitsToFloat(0) || repovuza.drain$ == ofodomev || repovuza.touring$ != ofodomev) {
         repovuza.believe$ = repovuza.drain$;
         repovuza.touring$ = (float)ofodomev;
      }

      if (repovuza.teacher$ == Float.intBitsToFloat(0) || repovuza.teacher$ == zubaficu || repovuza.lawyers$ != zubaficu) {
         repovuza.murder$ = repovuza.teacher$;
         repovuza.lawyers$ = (float)zubaficu;
      }

   }

   public void _suffer(float izurisir) {
      dimilito.flickr$ = (float)izurisir;
   }

   public void _posting(float which) {
      service.drain$ = (float)which;
   }

   public void _existed(float panigulu) {
      megopaci.teacher$ = (float)panigulu;
   }

   public void _nissan(float rated, float either) {
      listen._achieved((float)rated, (float)either, Float.intBitsToFloat(0));
      listen.flickr$ = AnimationUtils._sudan(listen.equity$, listen.flickr$, (float)rated, 1.0F);
      listen.drain$ = AnimationUtils._sudan(listen.believe$, listen.drain$, (float)either, 1.0F);
   }

   public void _escape(float optics, float vernon, float catalog) {
      explore._achieved((float)optics, (float)vernon, Float.intBitsToFloat(0));
      explore.flickr$ = AnimationUtils._sudan(explore.equity$, explore.flickr$, (float)optics, (float)catalog);
      explore.drain$ = AnimationUtils._sudan(explore.believe$, explore.drain$, (float)vernon, (float)catalog);
   }

   public void _intense(float terry, float loaded, float falls, float property) {
      aquarium._achieved((float)terry, (float)loaded, Float.intBitsToFloat(0));
      aquarium.flickr$ = AnimationUtils._sudan(aquarium.equity$, aquarium.flickr$, (float)terry, (float)falls);
      aquarium.drain$ = AnimationUtils._sudan(aquarium.believe$, aquarium.drain$, (float)loaded, (float)property);
   }

   public void _costume(float sunshine, float cologne, float legal, float speaking, float counters, float surgery) {
      realty._achieved((float)sunshine, (float)cologne, (float)legal);
      realty.flickr$ = AnimationUtils._sudan(realty.equity$, realty.flickr$, (float)sunshine, (float)speaking);
      realty.drain$ = AnimationUtils._sudan(realty.believe$, realty.drain$, (float)cologne, (float)counters);
      realty.teacher$ = AnimationUtils._sudan(realty.murder$, realty.teacher$, (float)legal, (float)surgery);
   }

   public float _turbo() {
      return gileluse.flickr$;
   }

   public float _tattoo() {
      return pilapamo.drain$;
   }

   public float _owner() {
      return wallace.teacher$;
   }
}
