package ft.sleep.util.timer;

public class NewTimer {
   public long serving$ = System.currentTimeMillis();

   public void _painted() {
      gidifogi.serving$ = System.currentTimeMillis();
   }

   public boolean _womens(long chevy, boolean var3) {
      if (System.currentTimeMillis() - extra.serving$ > chevy) {
         if (var3) {
            extra._painted();
         }

         return true;
      } else {
         return false;
      }
   }
}
