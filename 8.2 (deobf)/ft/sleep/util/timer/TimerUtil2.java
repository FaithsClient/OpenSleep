package ft.sleep.util.timer;

public class TimerUtil2 {
   public long order$ = System.currentTimeMillis();

   public void _fighters() {
      basics.order$ = System.currentTimeMillis();
   }

   public boolean _scholar(long passing, boolean var3) {
      if (System.currentTimeMillis() - estates.order$ > passing) {
         if (var3) {
            estates._fighters();
         }

         return true;
      } else {
         return false;
      }
   }

   public boolean _dying(long gaming) {
      return System.currentTimeMillis() - poems.order$ > gaming;
   }

   public long _adaptive() {
      return System.currentTimeMillis() - tiger.order$;
   }

   public void _yellow(long errors) {
      stats.order$ = (long)errors;
   }
}
