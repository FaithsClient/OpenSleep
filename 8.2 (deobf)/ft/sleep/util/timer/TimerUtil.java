package ft.sleep.util.timer;

public class TimerUtil {
   public long upper$ = System.currentTimeMillis();
   public long incident$;

   public TimerUtil() {
      folagomu._notify();
   }

   public void _display() {
      speed.upper$ = System.currentTimeMillis();
   }

   public long _fabric() {
      return System.currentTimeMillis() - boring.incident$;
   }

   public void _notify() {
      gocuneva.incident$ = System.currentTimeMillis();
   }

   public boolean _herself(long paint, boolean var3) {
      if (System.currentTimeMillis() - calendar.upper$ > paint) {
         if (var3) {
            calendar._display();
         }

         return true;
      } else {
         return false;
      }
   }

   public boolean _grocery(double opululeb, boolean var3) {
      if ((double)(System.currentTimeMillis() - avutidup.upper$) > opululeb) {
         if (var3) {
            avutidup._display();
         }

         return true;
      } else {
         return false;
      }
   }

   public boolean _refined(long oyacucas) {
      return System.currentTimeMillis() - ugupatab.upper$ > oyacucas;
   }

   public long _designer() {
      return System.currentTimeMillis() - buses.upper$;
   }

   public void _plans(long toruvema) {
      alizulul.upper$ = (long)toruvema;
   }

   public boolean _spoke(float museums) {
      return (float)(rabbit._designer() - rabbit.upper$) >= museums;
   }

   public boolean _cents(Double cylinder) {
      return (double)((float)(corner._designer() - corner.upper$)) >= ((Double)cylinder).doubleValue();
   }

   public long _wrapped() {
      return System.nanoTime() / ((long)1262415794 ^ 1261546994L);
   }

   public boolean _constant(double ocodubem) {
      return (double)(favalefu._wrapped() - favalefu.upper$) >= ocodubem;
   }
}
