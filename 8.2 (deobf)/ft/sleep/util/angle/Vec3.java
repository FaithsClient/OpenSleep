package ft.sleep.util.angle;

public class Vec3 {
   public double bread$;
   public double adams$;
   public double walnut$;

   public Vec3(double zedalido, double tafepici, double var5) {
      izitinen.bread$ = (double)zedalido;
      izitinen.adams$ = (double)tafepici;
      izitinen.walnut$ = var5;
   }

   public double _largely() {
      return vendor.bread$;
   }

   public double _duties() {
      return sidonisu.adams$;
   }

   public double _textile() {
      return zumafisu.walnut$;
   }

   public Vec3 _shopper(double inagumep, double egulicot, double var5) {
      return new Vec3(ayatomor.bread$ + inagumep, ayatomor.adams$ + egulicot, ayatomor.walnut$ + var5);
   }

   public Vec3 _salmon() {
      return new Vec3(Math.floor(ufarobef.bread$), Math.floor(ufarobef.adams$), Math.floor(ufarobef.walnut$));
   }

   public double _hosted(Vec3 peyofeca) {
      return Math.pow(((Vec3)peyofeca).bread$ - avuvasas.bread$, 2.0D) + Math.pow(((Vec3)peyofeca).adams$ - avuvasas.adams$, 2.0D) + Math.pow(((Vec3)peyofeca).walnut$ - avuvasas.walnut$, 2.0D);
   }

   public Vec3 _aberdeen(Vec3 egivebop) {
      return cisenefa._shopper(((Vec3)egivebop)._largely(), ((Vec3)egivebop)._duties(), ((Vec3)egivebop)._textile());
   }

   public net.minecraft.util.Vec3 _matthew() {
      return new net.minecraft.util.Vec3(crystal.bread$, crystal.adams$, crystal.walnut$);
   }

   public String toString() {
      return "[" + yeyufacu.bread$ + ";" + yeyufacu.adams$ + ";" + yeyufacu.walnut$ + "]";
   }
}
