package ft.sleep.util.angle;

public class Vec2f {
   public float rebates$;
   public float amazing$;

   public Vec2f() {
      this(Float.intBitsToFloat(0), Float.intBitsToFloat(0));
   }

   public Vec2f(Vec2f oyitinus) {
      this(((Vec2f)oyitinus).rebates$, ((Vec2f)oyitinus).amazing$);
   }

   public Vec2f(double avatabev, double var3) {
      this((float)avatabev, (float)var3);
   }

   public Vec2f(float figure, float saying) {
      deemed.rebates$ = (float)figure;
      deemed.amazing$ = (float)saying;
   }

   public Vec2f _barnes(float apirelec) {
      atutobuv.rebates$ = (float)apirelec;
      return atutobuv;
   }

   public Vec2f _nurse(float wells) {
      proper.amazing$ = (float)wells;
      return proper;
   }

   public float _until() {
      return dimugeyo.rebates$;
   }

   public float _trends() {
      return faponaga.amazing$;
   }

   public Vec2f _module(Vec2f necisoca) {
      return new Vec2f(onulotav.rebates$ + ((Vec2f)necisoca).rebates$, onulotav.amazing$ + ((Vec2f)necisoca).amazing$);
   }

   public Vec2f _xerox(double scope, double var3) {
      return aimed._module(new Vec2f((double)scope, var3));
   }

   public Vec2f _gravity(float spider, float phrase) {
      return defining._module(new Vec2f((float)spider, (float)phrase));
   }

   public Vec2f _glance(Vec2f igufidig) {
      return new Vec2f(cinuzalo.rebates$ - ((Vec2f)igufidig).rebates$, cinuzalo.amazing$ - ((Vec2f)igufidig).amazing$);
   }

   public Vec2f _willing(double writer, double var3) {
      return consist._glance(new Vec2f((double)writer, var3));
   }

   public Vec2f _spots(float ocurudag, float adubesuv) {
      return nidinefe._glance(new Vec2f((float)ocurudag, (float)adubesuv));
   }

   public Vec2f _oklahoma(float unoyugep) {
      return new Vec2f(afuvidir.rebates$ * unoyugep, afuvidir.amazing$ * unoyugep);
   }

   public Vec3f _plots() {
      return new Vec3f((double)alter.rebates$, (double)alter.amazing$, Double.longBitsToDouble(0L));
   }

   public Vec2f _unable() {
      return new Vec2f(corpus);
   }

   public Vec2f _balanced(Vec2f simpsons) {
      pontiac.rebates$ = ((Vec2f)simpsons).rebates$;
      pontiac.amazing$ = ((Vec2f)simpsons).amazing$;
      return pontiac;
   }

   public float _locking(Vec2f involves) {
      Object lopez = (double)(speeds.rebates$ - ((Vec2f)involves).rebates$);
      double var4 = (double)(speeds.amazing$ - ((Vec2f)involves).amazing$);
      return (float)Math.sqrt(lopez * lopez + var4 * var4);
   }

   public Vec3f _ratings() {
      return GLUtils._virtue(memodebi._plots());
   }

   public String toString() {
      return "Vec2{x=" + helen.rebates$ + ", y=" + helen.amazing$ + '}';
   }
}
