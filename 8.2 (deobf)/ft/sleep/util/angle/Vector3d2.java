package ft.sleep.util.angle;

public class Vector3d2 {
   public double travel$;
   public double years$;
   public double voted$;

   public Vector3d2(double colonial, double climb, double var5) {
      fisher.travel$ = (double)colonial;
      fisher.years$ = (double)climb;
      fisher.voted$ = var5;
   }

   public Vector3d2 _diploma(double profile, double previous, double var5) {
      return new Vector3d2(mazda.travel$ + profile, mazda.years$ + previous, mazda.voted$ + var5);
   }

   public Vector3d2 _delayed(Vector3d2 azecimul) {
      return igibitif._diploma(((Vector3d2)azecimul).travel$, ((Vector3d2)azecimul).years$, ((Vector3d2)azecimul).voted$);
   }

   public Vector3d2 _minolta(double ledasanu, double oputemur, double var5) {
      return ozeyogas._diploma((double)(-ledasanu), (double)(-oputemur), -var5);
   }

   public Vector3d2 _cassette(Vector3d2 desired) {
      return despite._diploma(-((Vector3d2)desired).travel$, -((Vector3d2)desired).years$, -((Vector3d2)desired).voted$);
   }

   public double _gibson() {
      return Math.sqrt(arufayag.travel$ * arufayag.travel$ + arufayag.years$ * arufayag.years$ + arufayag.voted$ * arufayag.voted$);
   }

   public double _adrian() {
      return roots.travel$;
   }

   public double _monroe() {
      return named.years$;
   }

   public double _alien() {
      return winter.voted$;
   }

   public Vector3d2 _sound(double atasitul) {
      return new Vector3d2(ideticib.travel$ * atasitul, ideticib.years$ * atasitul, ideticib.voted$ * atasitul);
   }

   public boolean equals(Object arabia) {
      if (!(arabia instanceof Vector3d2)) {
         return false;
      } else {
         Object gauge = (Vector3d2)arabia;
         return Math.floor(laden.travel$) == Math.floor(gauge.travel$) && Math.floor(laden.years$) == Math.floor(gauge.years$) && Math.floor(laden.voted$) == Math.floor(gauge.voted$);
      }
   }
}
